import { Routes } from '@angular/router';
import { ComprasComponent } from './compras/compras.component';  // Importe o componente Compras

export const routes: Routes = [
  { path: '', component: ComprasComponent },  // Rota padrão
];
